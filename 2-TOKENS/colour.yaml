# Colour Tokens
# status: EMPTY — authored in Claude Design
# Purpose: Machine-readable colour tokens. Semantic naming (colour.surface.base, not colour.grey-100). Dark/light variants. Derived from tokens-reference.css in SEED/.
#
# Authored in Claude Design. See /BRIEF.md §5 for target structure.
