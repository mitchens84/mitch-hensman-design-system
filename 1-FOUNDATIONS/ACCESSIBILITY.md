---
title: Accessibility
status: EMPTY — authored in Claude Design
---

<!-- Purpose: WCAG AA floor from the 2024 philosophy; elaboration upward. Expected coverage when authored:
  (a) Contrast — ratios per semantic token pair (text on surface, link on background, focus on surface); explicit pass/fail per pair
  (b) Keyboard navigation — focus ring specification, tab order, skip-to-content, keyboard shortcuts where they exist
  (c) Screen-reader semantics — heading hierarchy, landmark roles, ARIA patterns, live regions where interactive
  (d) Cognitive accessibility — reading level target, progressive disclosure rules, chunking, plain-language defaults
  (e) Motion sensitivity — prefers-reduced-motion compliance required; reduced-motion alternatives for any authored motion token
  (f) Language — lang attribute per-page; Thai and non-Latin script rendering; direction (ltr/rtl) handling if applicable
Elaborate upward from the floor; never downward. -->

<!-- EMPTY — authored in Claude Design. See /BRIEF.md §5 for target structure. -->

# Accessibility

*Scaffold — content authored in Claude Design during operator-driven sessions.*
